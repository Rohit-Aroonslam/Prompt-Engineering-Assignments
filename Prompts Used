This document contains the prompts used to generate the Python code and analysis throughout the assignment. The prompts were refined iteratively to improve accuracy, reduce hallucinations, and ensure the generated code worked correctly within Google Colab.

Step 1 – Data Exploration
Prompt
Load historical stock price data for Apple Inc. (AAPL) from 2020-01-01 to 2023-01-01 using Yahoo Finance.

Requirements:
- Download the data using yfinance.
- Store the dataset in a DataFrame named data.
- Display the first 10 rows.
- Display summary statistics using data.describe().
- Print the highest closing price, lowest closing price, average closing price, and total number of trading days.
- Keep the data in memory for the next steps.
Step 2 – Feature Engineering
Prompt
Using the existing DataFrame named data from the previous step:

1. Print the current column names.

2. If the DataFrame contains MultiIndex columns from Yahoo Finance, flatten them so that the final column names are:

Open
High
Low
Close
Volume

Use Adjusted Close as Close if it exists.

3. Create the following features:

- MA_5 (5-day Moving Average)
- MA_10 (10-day Moving Average)
- MA_50 (50-day Moving Average)
- Daily_Returns (percentage daily return)
- Volatility_10 (10-day rolling standard deviation of Daily_Returns)

4. Remove rows containing missing values using:

data.dropna(inplace=True)

5. Print the updated column names.

6. Display the first 10 rows of the processed DataFrame.

Do not create unnecessary DataFrames unless required.
Use only pandas and numpy.
Step 3.1 – Linear Regression
Prompt
Using the existing feature-engineered DataFrame named data:

1. Print a heading:
STEP 3.1 – LINEAR REGRESSION MODEL

2. Create a target column named Tomorrow_Close by shifting the Close column forward by one day.

3. Create a new DataFrame called model_data by removing rows with missing values.

4. Use the following predictor variables:

Open
High
Low
Close
Volume
MA_5
MA_10
MA_50
Daily_Returns
Volatility_10

5. Split the data chronologically using a 70% training and 30% testing split without shuffling.

6. Train a Linear Regression model.

7. Display:

- Mean Squared Error (MSE)
- Mean Absolute Error (MAE)
- Root Mean Squared Error (RMSE)
- R² Score

8. Display the performance metrics in a pandas DataFrame.

9. Plot Actual vs Predicted Tomorrow Close Prices using Matplotlib.

Use only the existing DataFrame and do not reload the dataset.
Step 3.2 – Random Forest Classification
Prompt
Using the existing feature-engineered DataFrame named data:

1. Print a heading:
STEP 3.2 – RANDOM FOREST CLASSIFICATION

2. Create a binary target column named Target:

Target = 1 if tomorrow's closing price is greater than today's closing price.
Target = 0 otherwise.

3. Remove rows containing missing values.

4. Use the following predictor variables:

Open
High
Low
Close
Volume
MA_5
MA_10
MA_50
Daily_Returns
Volatility_10

5. Split the data chronologically using a 70% training and 30% testing split without shuffling.

6. Train a Random Forest Classifier.

7. Display:

- Accuracy
- Confusion Matrix
- Classification Report
- Feature Importance

8. Plot the Feature Importance using Matplotlib.

Store the trained model as rf_model for use in the next step.
Step 4 – Model Evaluation & Prediction
Prompt
Using the previously trained Random Forest model (rf_model):

1. Download Apple (AAPL) stock price data from 2023-01-01 to 2024-01-01 using Yahoo Finance.

2. Apply exactly the same feature engineering process used during training.

3. Predict whether the stock price will increase or decrease.

4. Display:

- Accuracy
- Confusion Matrix
- Classification Report

5. Plot Actual vs Predicted stock price direction using Matplotlib.

Do not retrain the model.
Only evaluate the existing Random Forest model on the new unseen data.
Step 5 – Strategy Discussion
Prompt
Using ONLY the Random Forest Classification results generated in this notebook, write a complete discussion of the model's performance.

IMPORTANT INSTRUCTIONS

• Use ONLY the Accuracy, Confusion Matrix, Classification Report and Feature Importance generated by this notebook.
• Do NOT invent, estimate or assume any statistics.
• If a metric is unavailable, state that it is unavailable instead of making one up.
• Base every conclusion directly on the reported results.
• Do not compare the results with the sample answers provided in the assignment.
• If discussing overfitting or generalisation, use cautious academic language such as "may indicate" or "suggests" rather than making definitive claims.
• Write in formal academic language suitable for a university report.
• Use complete paragraphs with clear headings.
• Ensure the response is fully completed and does not stop before the Conclusion.
